{#/workout/new.html.twig#}

{% extends 'base.html.twig' %}

{% block title %}New Workout{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        input[type=text], input[type=datetime-local], select {
            background-color: #2c2c2c;
            color: #e0e0e0;
            border: 1px solid #444444;
            border-radius: 5px;
            padding: 8px;
            width: 100%;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="container">
        <div class="card">
            <h1>Create New Workout</h1>
            {{ form_start(form) }}
            <div class="form-group">
                {{ form_row(form.name) }}
            </div>
            <div class="form-group">
                {{ form_row(form.date) }}
            </div>
            <div class="form-group">
                {{ form_row(form.type) }}
            </div>
            {{ form_widget(form.save, {'attr': {'class': 'btn btn-primary', 'style': 'width: 8%;'}}) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
