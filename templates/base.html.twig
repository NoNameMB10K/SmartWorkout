<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}BASE{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
        <link rel="icon" href="{{ asset('images/mascot.png') }}">
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
</head>
<body>
{% block nav %}
<nav class="navbar">
    <ul class="nav-list">
        <li><a href="{{ path('app_home') }}" class="btn-nav">Home</a></li>
        <li><a href="{{ path('workouts_index') }}" class="btn-nav">Workouts</a></li>
        <li><a href="{{ path('exercises_index') }}" class="btn-nav">Exercises</a></li>
        <li><a href="{{ path('types_index') }}" class="btn-nav">Types</a></li>
    </ul>
    <div class="login-icon">
        {% if app.user is not empty %}
            <a href="{{ path('user_show') }}" class="btn-nav">Account</a>
            <a href="{{ path('app_logout') }}" class="btn-nav">Logout</a>
        {% else %}
            <a href="{{ path('app_login') }}" class="btn-nav">Login</a>
        {% endif %}
    </div>
</nav>
{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">SmartWorkout</h1>
        <div class="card">
            <div class="text-center mb-4">
                <img src="{{ asset('images/mascot.png') }}" alt="Mascot Image" class="img-fluid" style="max-width: 35%;">
            </div>
            <div class="card-body">
                <h1 class="card-title">Features</h1>
                <br>
                <ul>
                    <li>Track your gym progress effortlessly</li>
                    <li>Define custom exercises and categorize them into types such as muscle groups or exercise genres like weightlifting, calisthenics, cardio, etc.</li>
                    <li>Create custom workouts and add your exercises to them</li>
                    <li>Easily keep track of the number of reps and weights lifted</li>
                    <li>View workout statistics to monitor and track your progress over time</li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</body>
</html>
