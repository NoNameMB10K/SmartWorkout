<!DOCTYPE html>
<html>
<head>
    <title>Exercise</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
        }
        th {
            padding-top: 12px;
            padding-bottom: 12px;
            text-align: left;
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
<h1>Exercise</h1>
<table>
    <thead>
    <tr>
        <th>Name</th>
        <th>Link to youtube video</th>
        <th>Type</th>
    </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{ exercise.name }}</td>
            <td>{{ exercise.linkToVideo}}</td>
            <td>{{ exercise.type is not null ? exercise.type.name : 'N/A' }}</td>
        </tr>
    </tbody>
</table>
</body>
</html>
